# Depth-First Search (DFS) in Python
The Depth First Search (DFS) algorithm is a method for traversing or searching tree and graph data structures by exploring a branch as deeply as possible before backtracking. It has a time complexity of O(V + E).
## Run
```
python main.py
```

## Example Output
See screenshot below.
